---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "resume.fullname" . }}-backend-headless
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "resume.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  clusterIP: None
  selector:
    {{- include "resume.backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.targetPort }}
      protocol: TCP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "resume.fullname" . }}-backend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "resume.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  serviceName: {{ include "resume.fullname" . }}-backend-headless
  replicas: {{ .Values.backend.replicaCount }}
  selector:
    matchLabels:
      {{- include "resume.backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/system-prompt: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "resume.backend.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.backend.podSecurityContext | default .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          {{- with .Values.backend.securityContext | default .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: openai-api-key
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: jwt-secret
            - name: TURNSTILE_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: turnstile-secret
            - name: TURNSTILE_SITE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: turnstile-site-key
            - name: OPENAI_MODEL
              value: {{ .Values.backend.env.openaiModel | quote }}
            - name: PORT
              value: {{ .Values.backend.env.port | quote }}
            - name: USE_LOCAL_PIPELINE
              value: {{ .Values.backend.env.useLocalPipeline | quote }}
            - name: LOCAL_LLM_URL
              value: {{ .Values.backend.env.localLLMURL | quote }}
            - name: TTS_URL
              value: {{ .Values.backend.env.ttsURL | quote }}
          ports:
            - name: http
              containerPort: {{ .Values.backend.service.targetPort }}
              protocol: TCP
          {{- with .Values.backend.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.backend.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: system-prompt
              mountPath: /app/data
              readOnly: true
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
        {{- if .Values.backend.tts.enabled }}
        - name: tts
          image: "{{ .Values.backend.tts.image.repository }}:{{ .Values.backend.tts.image.tag }}"
          imagePullPolicy: {{ .Values.backend.tts.image.pullPolicy }}
          env:
            - name: TTS_HOME
              value: voices
            - name: HF_HOME
              value: voices
          ports:
            - name: tts
              containerPort: 8000
              protocol: TCP
          volumeMounts:
            - name: tts-voices
              mountPath: /app/voices
          resources:
            {{- toYaml .Values.backend.tts.resources | nindent 12 }}
        {{- end }}
      volumes:
        - name: system-prompt
          configMap:
            name: {{ include "resume.fullname" . }}-system-prompt
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- if .Values.backend.tts.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: tts-voices
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.backend.tts.storage.size }}
        {{- if .Values.backend.tts.storage.storageClass }}
        storageClassName: {{ .Values.backend.tts.storage.storageClass }}
        {{- end }}
  {{- end }}
