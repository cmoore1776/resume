---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "resume.fullname" . }}-system-prompt
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "resume.labels" . | nindent 4 }}
data:
  system_prompt.txt: |
{{ .Values.systemPrompt.content | indent 4 }}
